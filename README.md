# @endge/integration-api

Stable, framework-neutral contracts for Endge integrations. The package does not depend on `@endge/core`; Endge supplies the runtime implementations of these interfaces.

Vue-specific visual definitions are available from `@endge/integration-api/vue` and keep Vue as an optional peer dependency.

